function n = luckynum(N)    isEven = @(value) mod(value, 2) == 0;  start = N;  if (isEven(N))    start = N + 1;
  endif      current = start;  while(!isLucky(current))      current += 2;  endwhile  n = current;endfunction